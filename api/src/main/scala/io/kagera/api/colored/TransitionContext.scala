package io.kagera.api.colored

/**
 * TODO add a mutable map that may be read / written to during the transition. ?? def data:
 * scala.collection.mutable.Map[String, Any]
 */
case class TransitionContext(id: java.util.UUID)
